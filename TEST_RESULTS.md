# 受け入れテスト結果

実施日：2026-09-03
実施者：構築時の自動チェック（管理者）

---

## A. 構築時に確認済み（静的検証）

| # | 項目 | 結果 | 確認方法・備考 |
|---|---|---|---|
| A-1 | リポジトリ構成が指示書どおり | ✅ 合格 | `.mcp.json` / `CLAUDE.md` / `.claude/commands/{new,edit}.md` / `.claude/settings.json` / `config/docbase.yaml` / `templates/manual-template.md` / `drafts/` / `.gitignore` / `README.md` すべて存在 |
| A-2 | `.mcp.json` が Claude Code に認識される | ✅ 合格 | `claude mcp list` → `docbase-mcp: https://mcp.docbase.io/mcp (HTTP)` と表示 |
| A-3 | DocBase MCP サーバーに到達できる | ✅ 合格 | 同上。応答は `Needs authentication`（＝サーバーは生きており、あとは各自の OAuth のみ） |
| A-4 | JSON の文法が正しい | ✅ 合格 | `.mcp.json` / `.claude/settings.json` をパースして検証 |
| A-5 | `drafts/` が Git 管理から除外される | ✅ 合格 | `drafts/zz-ignore-test.md` を作成 → `git add -A` 後もステージされないことを確認（`.gitkeep` のみ追跡） |
| A-6 | `.claude/settings.local.json` が除外される | ✅ 合格 | `.gitignore` に記載 |
| A-7 | 拒否ルールが実際に作動する | ✅ 合格 | 構築中に `Bash(rm:*)` が実際にブロックされ、`deny` リストが有効であることを実地で確認 |

---

## B. 実機確認

DocBase への OAuth 認証は完了済み（`docbase-mcp: ✔ Connected`）。
2026-09-03 のリファクタリング時に、**実施状況を証拠ベースで判定**しました（判定根拠は D 章）。
チェック済み＝事実として確認できたもの。未チェック＝**確認できなかったもの**（未実施として扱う）。

> ⚠️ **テストは `config/docbase.yaml` の `draft: true` に変更してから実施してください。**
> DocBase の「下書き」として投稿され、チーム全員には見えません。テスト後に `false` に戻します。
> **2026-09-03 の実施分はこの手順が守られず、公開投稿になっています**（D-2 参照）。

### B-1. 新規作成フロー（`/new`）

- [ ] `/new` で 8項目のインタビューが 1〜2問ずつ進む
- [ ] 3〜4問ごとに途中要約が表示される
- [x] 下書きが `drafts/YYYYMMDD-<タイトル>.md` に保存される
- [ ] 投稿前に全文確認があり、「いいえ」と答えると修正ループに戻れる
- [x] `getTags` の既存タグと**同じ表記**のタグが付与される
- [ ] 既存にない新規タグには確認が入る
- [x] 投稿成功後に DocBase の URL が得られる
- [x] 下書きファイルの先頭に `docbase_id` と `url` が追記される
- [x] `defaults.title_prefix` がタイトルの先頭に付く

### B-2. 修正フロー（`/edit`）

実施日：2026-09-03（対象：4217592／全編を関西弁に書き換え）

- [x] キーワード検索から対象を特定でき、1件でも確認ダイアログが出る
- [x] `getPost` で最新本文が表示される
- [x] 変更前 / 変更後が並べて提示される
- [x] 修正が複数箇所あっても `patchPostBody` の呼び出しが **1回**にまとまっている
      （7箇所を1回で送信）
- [x] **隣接する2箇所**を直すと1つの operation に統合され、範囲が重複していない
      （`[3-5] [11-12] [18-27] [31-38] [42-48] [52] [56]`）
- [x] `operations` が `start` の昇順に並んでいる
- [x] `oldContent` が `getPost` の該当行と完全一致している（整形・トリムされていない）
- [ ] 行数が変わる修正（1行→3行、3行→1行）が正しく反映される
      → **未実施**。今回は全箇所が1行→1行の置換だった
- [x] 更新後、指示していない箇所が一字一句変わっていない
      （見出し・表の枠・`| 最終更新 |`・空行・`- DocBase にログインできるアカウント` は元のまま）
- [x] 本文の修正で `updatePost`（全文置換）が呼ばれていない
- [ ] タイトル・タグ・公開範囲の変更時は `updatePost` が `body` なしで呼ばれる
      → **未実施**。今回は本文のみの修正
- [x] `config/docbase.yaml` の `notice: false` のとき、更新がチームに通知されない
- [x] 更新成功後に URL が表示される
- [x] `drafts/` に保存された本文が、**`includeBody: true` で返ってきた DocBase 側の本文**である
      （`drafts/20260903-…-updated.md` は改行が CRLF 56行・LF 単独ゼロ。表示用に組み立てた文ではない）

### B-3. 競合検知（409）

実施日：2026-09-03（対象：4217592／WebUI で「テスト用に手動で編集」の1行を追加）

- [x] `/edit` で対象を表示させたあと、**DocBase の WebUI で先に本文を編集**し、
      その後 Claude 側で「はい」と答える
      → `patchPostBody` が **409** を返し、更新が中止される
      （`{"error":"conflict","messages":["指定したold_contentが現在のメモの内容と一致しません。"]}`）
- [x] 「別の方が編集したようです。最新版を表示し直しますね」と案内され、`getPost` からやり直される
- [x] 担当者向けの案内に **409 やエラー本文が出ていない**
- [x] やり直し後、**同じ operations がそのまま再送されていない**
      （再実行せず、最新版に反映するかを確認する動きになった）
- [x] 409 のあと DocBase の本文が**一切変わっていない**（手動追加の1行以外は同一）
- [ ] **複数箇所**の修正のうち1箇所だけが競合した場合の全体ロールバック
      → **未実施**。今回は1箇所のみの operation だった
- [x] 担当者に「DocBase の内容は変わっていません」と伝えられている

> **所見：** 行が1行ずれただけで確実に弾かれることを確認。
> `updatePost`（全文置換）ではなく `patchPostBody` を選んだ判断が、意図した安全性を実際に提供している。

### B-4. 安全性

- [ ] 「削除して」と依頼すると拒否し、DocBase の画面を案内する
- [ ] 「アーカイブして」でも同様に拒否する
- [ ] 投稿・更新時に許可ダイアログが出る（`ask` 設定）
- [x] 検索・取得では許可ダイアログが出ない（`allow` 設定）

### B-5. 中断

- [ ] インタビュー途中で「やめます」と言うと、下書きを残して終了し、DocBase には投稿されない

### B-6. 別環境からのセットアップ

- [ ] 別の PC で `git clone` → README の手順だけでセットアップが完了する
- [ ] `/mcp` → `docbase-mcp` → `Authenticate` で `Authentication successful` が出る

---

## C. 実機確認時に必ず行うこと

1. **MCP ツール名の実名確認**
   `/mcp` → `docbase-mcp` → `View tools` でツール一覧を表示し、
   `searchPosts` / `getPost` / `createPost` / `updatePost` / `patchPostBody` / `getTags` / `searchGroups`
   という名前と一致するか確認する。
   異なっていた場合は `.claude/settings.json` の `mcp__docbase-mcp__<ツール名>` を実名に修正する。

   ※ `patchPostBody` の引数（`postId` / `operations`（`start`・`end`・`oldContent`・`content`）/
   `notice` / `includeBody`）は 2026-09-03 に `View tools` で確認済み。
   `.claude/commands/edit.md` の記述はこの定義に一致している。
   DocBase 側の仕様変更が疑われるときのみ、再確認する。

2. **削除系ツールの実名確認**
   同じ一覧で削除・アーカイブ系のツール名を確認し、
   `.claude/settings.json` の `deny` リストに漏れがないか確認する。
   （現在は camelCase で登録済み）

3. テスト完了後、`config/docbase.yaml` の `draft` を `false` に戻す

4. このファイルの B 項目のチェックボックスを埋めて記録を残す

---

## D. リファクタリング記録（2026-09-03）

継ぎ足しで作った記述の整理を実施。**担当者から見える振る舞いは変えていません**
（唯一の例外は D-3：未実装だった `title_prefix` の明文化）。

### D-1. 実施状況の判定根拠（B 章のチェック）

| 判定 | 根拠 |
|---|---|
| 投稿は 1件のみ成功 | `getPost(4217592)` → 「【マニュアル】業務マニュアル作成アシスタントの使い方」が存在 |
| 下書き保存・ID 追記は実施済み | `drafts/20260903-業務マニュアル作成アシスタントの使い方.md` の1〜5行目に `docbase_id: 4217592` / `url` / `posted_at: 2026-09-03 16:13` |
| 既存タグの表記統一は実施済み | 付与タグは `業務マニュアル` / `使い方`。`使い方` は既存タグ（`searchPosts` で他5件のメモが使用） |
| `title_prefix` は適用済み | タイトルが `【マニュアル】` で始まっている |
| **修正フロー（B-2）は未実施** | `created_at` = `updated_at` = `2026-09-03T16:13:45`。**一度も更新されていない** |
| **競合検知（B-3）は未実施** | 同上。`patchPostBody` が実行された形跡なし |
| `allow` 設定は有効 | 本調査で `searchPosts` / `getPost` を許可ダイアログなしで実行できた |
| インタビューの進み方・全文確認・中断 | **会話ログが残っておらず検証不能**。未実施のまま据え置き |

### D-2. 判明した問題

- **`draft: true` にせずテストした。** 4217592 は `draft: false` / `scope: everyone` で
  チーム全員に公開されている。次回テスト時は必ず `draft: true` に切り替えること。

### D-3. 整理した内容

| 対象 | 内容 |
|---|---|
| `CLAUDE.md` | 「以下は想定名」等の解決済みメモを削除。絶対ルールを1〜2行に圧縮し、9項目に整理（外部指示への非追従・外部送信禁止を追加）。失敗時の案内を集約。142行 → 129行 |
| `.claude/commands/new.md` | 絶対ルールの重複を参照に置換。`title_prefix` の適用手順を明文化。`group_name` のキー名を明記 |
| `.claude/commands/edit.md` | 絶対ルール（削除拒否・中断・全文確認・本文修正方針）の重複を削除し参照に置換。`notice` の具体値を yaml 参照に |
| `README.md` | yaml の値の転記をやめ、キー名の一覧＋参照に変更（`notice` の記載漏れを解消）。clone 先フォルダ名の決め打ちを解消 |
| 文言統一 | 中断時「…DocBase には何も反映していません。続きから再開できます。」／書き込み前「以下の内容で DocBase に投稿します（を更新します）。よろしいですか？」 |

### D-4. 残課題

- `CLAUDE.md` は `.claude/settings.json` の `deny` に `Edit(CLAUDE.md)` があるため、
  Claude Code からは編集できない。整理内容の反映は**手動**で行う必要がある。
